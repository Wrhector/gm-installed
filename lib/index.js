"use strict";

/**
 * gmInstalled
 * Check if GraphicsMagick is installed.
 *
 * @name gmInstalled
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function gmInstalled (a, b) {
    return a + b;
};